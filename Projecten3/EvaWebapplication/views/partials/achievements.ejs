<div class="page-header">
    <h1>Achievements</h1>
</div>

<div>
    <div ng-repeat="ach in achievements">
        <span>{{ach.title}}</span>
        <span>{{ach.description}}</span>
        <span>{{ach.achievementType}}</span>
        
    </div>
</div>